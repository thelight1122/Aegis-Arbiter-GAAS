# Required on the T4 VM — copy to .env and fill in values
# Or set these as environment variables / Docker --env-file

# Required: generate with: openssl rand -hex 32
JWT_SECRET=your-secret-here

# Server port
PORT=8787

# Optional: Speech-to-text service URL
# AEGIS_STT_URL=http://localhost:8000/transcribe
