# Codex Locks

The following elements are immutable within this branch:

- Canon documents in codex/canon/
- Protocol definitions in codex/protocols/
- Append-only paths: codex/artifacts/sssp/, evidence/runs/, peer_tensor storage
- Identifier formats for SSSP, ADR, and Findings

Changes must be introduced as a new versioned branch with explicit migration notes.
